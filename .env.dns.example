# DNS Configuration Environment Variables
# Copy this file to .env.dns and fill in your actual values

# Vercel Configuration (Required)
VERCEL_TOKEN=your_vercel_token_here
VERCEL_TEAM_ID=your_team_id_here
VERCEL_PROJECT_ID=your_project_id_here

# DNS Provider Configuration
# Options: vercel, cloudflare, namecheap, godaddy
DNS_PROVIDER=vercel

# Cloudflare Configuration (if using Cloudflare)
CLOUDFLARE_EMAIL=your_email@example.com
CLOUDFLARE_API_KEY=your_cloudflare_api_key
CLOUDFLARE_ZONE_ID=your_zone_id

# Namecheap Configuration (if using Namecheap)
NAMECHEAP_API_USER=your_api_user
NAMECHEAP_API_KEY=your_api_key
NAMECHEAP_CLIENT_IP=your_client_ip

# GoDaddy Configuration (if using GoDaddy)
GODADDY_API_KEY=your_api_key
GODADDY_API_SECRET=your_api_secret

# Domain Configuration
DOMAIN=itsdifferentproductions.com
SUBDOMAIN=docs
FULL_DOMAIN=docs.itsdifferentproductions.com

# Deployment Configuration
DOCS_BRANCH=main
DOCS_BUILD_COMMAND=npm run docs:build
DOCS_OUTPUT_DIRECTORY=docs

# Monitoring and Notifications
SLACK_WEBHOOK_URL=your_slack_webhook_url
DISCORD_WEBHOOK_URL=your_discord_webhook_url
EMAIL_NOTIFICATION=your_email@example.com
